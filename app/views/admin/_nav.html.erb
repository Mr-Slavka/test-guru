
  <div class="nav nav-admin">
    <span class="nav-admin title"><%= t('admin.nav.header') %></span>

    <div class="nav-admin user">
      <b><%= current_user.email %></b> |
      <%= link_to t('admin.nav.logout'), destroy_user_session_path, method: :delete %>

    </div>
    <% flash[:notice] = "#{t('admin.nav.welcome')}, #{current_user.first_name} #{current_user.last_name}!" %>

  </div>