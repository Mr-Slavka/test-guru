
  <tr>
    <td><%= test.title %></td>
    <td><%= test.level %></td>
    <td><%= test.questions.count %></td>
    <td><%= link_to t('helpers.submit.test.show'), admin_test_path(test), method: :get %></td>
    <td><%= link_to t('helpers.submit.test.edit'), edit_admin_test_path(test), method: :get %></td>
    <td><%= link_to t('helpers.submit.test.delete'), admin_test_path(test),
                              method: :delete,
                              data: { confirm: t('helpers.submit.test.confirm') } %></td>

  </tr>